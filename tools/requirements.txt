# Runtime dependencies for the checks in `just check`.
#
# The checkers themselves are no longer in this repository. They are general --
# nothing in them knows about this book -- so they live in `linji-tools` and are
# configured from the [tool.*] sections of pyproject.toml. What stays here is
# tools/anchors.py, the adapter that teaches them Watson's note markup.
#
# Pinned exactly. A checker that changes behaviour between two runs of CI is
# worse than one that is a version behind.
linji-tools==0.1.0
